import React, { FC } from 'react'

interface IProps {
  couponInfoList: string[]
}

/**
 * 售后打款组件
 * @param props 
 * @returns 
 */
const AftersaleDax: FC<IProps> = ({ couponInfoList }) => {
  return (
    <div>sasa</div>
  )
}

export default AftersaleDax
