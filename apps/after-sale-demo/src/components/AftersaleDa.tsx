import React, { FC } from 'react'

interface IProps {
  couponInfoList: string[]
}

/**
 * 售后打款组件2
 * @param props 
 * @returns 
 */
export default ({ couponInfoList }: IProps) => {
  return (
    <div>sasa</div>
  )
}